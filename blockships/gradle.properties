# Pinned defCoreLib build. SINGLE SOURCE OF TRUTH — two consumers read this line:
#   * blockships/build.gradle.kts    -> compile classpath (Gradle project property)
#   * the repo-root Makefile         -> runtime plugin jar, fetched from
#       https://jitpack.io/com/github/def9a2a4/defCoreLib/<ref>/defCoreLib-<ref>.jar
# BlockShips' plugin.yml has a hard `depend: [DefCoreLib]`, so both matter and must match.
#
# Bumping: push defCoreLib, warm the JitPack build for the new SHA
#   curl -s https://jitpack.io/api/builds/com.github.def9a2a4/defCoreLib/<sha>   # until "ok"
# then edit this line. Ad-hoc override: -PdefCoreLibRef=<sha> / make DEFCORELIB_REF=<sha>.
defCoreLibRef=beb1bb7
